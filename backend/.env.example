# Server
PORT=5000
NODE_ENV=development

# Contact form email delivery (optional - if unset, submissions are only logged)
CONTACT_TO_EMAIL=info@godslightsolars.com
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-smtp-user
SMTP_PASS=your-smtp-password
